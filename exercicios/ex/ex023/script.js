let dol = Number(window.prompt('Qual a atual cotação do dólar?'))

function converter() {
    let res = document.getElementById('res')
    let cotacaoAtual = dol.toLocaleString('pt-BR', {style:'currency', currency: 'BRL'})
    let brl = Number(window.prompt('Quanto você tem em dinheiro?'))
    let numeroDolar = 

}